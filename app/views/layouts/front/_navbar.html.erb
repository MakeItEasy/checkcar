    <!-- Fixed navbar -->
    <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container wrapped-content">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to t('view.site.front_title'), root_path, {class: 'navbar-brand', style: "color:white;"} %>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li class="<%= current_nav_class(root_path) %>">
              <%= link_to t('view.label.homepage'), root_path %>
            </li>
            <% @catagories.each do |catagory|  %>
              <li class="<%= current_nav_class(front_catagory_path(catagory)) %>">
                <%= link_to catagory.name, front_catagory_path(catagory) %>
              </li>
            <% end %>
            <li class="<%= current_nav_class(front_stations_path)%>">
              <%= link_to Station.model_name.human, front_stations_path %>
            </li>
            <li class="<%= current_nav_class(front_question_index_faq_path)%> <%= current_nav_class(front_question_index_uaq_path)%>">
              <%= link_to "提问留言", front_question_index_faq_path %>
            </li>
          </ul>
          <div id="user-action" class="pull-right">
            <% if current_user %>
              <div class="btn-group">
                  <a type="button" class="btn btn-success btn-sm" href="<%= front_user_root_path %>">
                    <%= current_user.show_name %>
                  </a>
                  <button type="button" class="btn btn-success btn-sm dropdown-toggle" data-toggle="dropdown">
                      <span class="caret"></span>
                      <span class="sr-only">Toggle Dropdown</span>
                  </button>
                  <ul class="dropdown-menu" role="menu">
                      <li><%= link_to "我的控制台", front_user_root_path %></li>
                      <li><a href="#">个人信息</a></li>
                      <li><%= link_to "修改密码", edit_user_registration_path %></li>
                      <li class="divider"></li>
                      <li><%= link_to t('view.action.sign_out'), destroy_user_session_path, {method: :delete} %></li>
                  </ul>
              </div>
            <% else %>
              <a id="btn_sign_in" class="btn btn-danger btn-sm"><%= t('view.action.sign_in')%></a>
              <a style="margin: 0px 8px;">|</a>
              <%= link_to t('view.action.sign_up'), new_user_registration_path,
                class: "btn btn-success btn-sm" %>
            <% end %>
          </div>
        </div><!--/.nav-collapse -->
      </div>
    </div>
    <!-- END Fixed navbar -->

  <% if !current_user %>
    <%= render "front/common/sign_in_modalbox" %> 
  <% end %>
