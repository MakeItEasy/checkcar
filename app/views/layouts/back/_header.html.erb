    <% if current_admin.system_admin? %>
      <%= link_to t('view.site.back_title'), back_system_root_path, {class: 'logo'} %>
    <% else %>
      <a class="logo" href="<%= back_mystation_root_path %>">
        <% if current_admin.station.status_reviewed? %>
        <i class="ion ion-model-s"></i> 
        <% else %>
        <span class="badge" style="background-color: red;"><%= current_admin.station.status_text%></span>
        <% end %>
        <%= current_admin.station.name%>
      </a>
    <% end %>
    <!-- Header Navbar: style can be found in header.less -->
    <nav class="navbar navbar-static-top" role="navigation">
      <!-- Sidebar toggle button-->
      <a href="#" class="navbar-btn sidebar-toggle" data-toggle="offcanvas" role="button">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <div class="navbar-right">
        <ul class="nav navbar-nav">
        <!-- Messages: style can be found in dropdown.less-->
        <li class="dropdown user user-menu">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <i class="glyphicon glyphicon-user"></i>
            <span><%= current_admin.name %> <i class="caret"></i></span>
          </a>
          <ul class="dropdown-menu">
          <!-- User image -->
          <li class="user-header bg-light-blue">
            <%# render_current_user_image %>
            <% if current_admin.sex_male? %>
              <%= image_tag "adminlte/avatar-male-1.png", class: "img-circle", alt:"User Image" %>
            <% else %>
              <%= image_tag "adminlte/avatar-female-1.png", class: "img-circle", alt:"User Image" %>
            <% end %>
            <p>
              <%= current_admin.name %>
              <small><%= truncate(render_current_admin_roles, length:40) %></small>
            </p>
          </li>
          <!-- Menu Body -->
          <!--
          <li class="user-body">
            <div class="col-xs-4 text-center">
            <a href="#">Followers</a>
            </div>
            <div class="col-xs-4 text-center">
            <a href="#">Sales</a>
            </div>
            <div class="col-xs-4 text-center">
            <a href="#">Friends</a>
            </div>
          </li>
          -->
          <!-- Menu Footer-->
          <li class="user-footer">
            <div class="pull-left">
              <%= link_to t('view.action.personal.show'), back_personal_show_path, class: "btn btn-default btn-flat" %>
            </div>
            <div class="pull-right">
              <%= link_to t('view.action.personal.password'), back_personal_password_path, class: "btn btn-default btn-flat" %>
            </div>
          </li>
          </ul>
        </li>
        <li>
          <%= link_to destroy_admin_session_path, {method: :delete} do %>
            <i class="glyphicon glyphicon-log-out"></i>
            <span><%= t('view.action.sign_out') %></span>
          <% end %>
        </li>
        </ul>
      </div>
    </nav>
